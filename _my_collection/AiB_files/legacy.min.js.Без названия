function BrowserDetection() {
    if (/Firefox[\/\s](\d+\.\d+)/.test(navigator.userAgent)) {
        var e = new Number(RegExp.$1);
        rint = 10;
        pulsion = 3
    } else if (/MSIE (\d+\.\d+);/.test(navigator.userAgent)) {
        var t = new Number(RegExp.$1);
        rint = 50;
        pulsion = 16
    } else if (/Chrome[\/\s](\d+\.\d+)/.test(navigator.userAgent)) {
        var n = new Number(RegExp.$1);
        rint = 50;
        pulsion = 16
    } else if (/Opera[\/\s](\d+\.\d+)/.test(navigator.userAgent)) {
        var r = new Number(RegExp.$1);
        rint = 50;
        pulsion = 16
    } else if (/Safari[\/\s](\d+\.\d+)/.test(navigator.userAgent)) {
        var i = new Number(RegExp.$1);
        rint = 50;
        pulsion = 16
    }
    WIDTH = window.innerWidth;
    if (WIDTH < 1024) {
        numberObject = 19e3;
        numberBall = 10;
        pulsion = 2;
        rint = 10
    }
}
BrowserDetection();
function draw() {
    con.clearRect(0, 0, WIDTH, HEIGHT);
    for (var e = 0; e < pxs.length; e++) {
        pxs[e].fade();
        pxs[e].move();
        pxs[e].draw()
    }
}
function Circle() {
    WIDTH = window.innerWidth;
    HEIGHT = window.innerHeight;
    this.s = {
        ttl: numberObject,
        xmax: speedX,
        ymax: speedY,
        rmax: radius,
        rt: pulsion,
        xdef: 960,
        ydef: 540,
        xdrift: 4,
        ydrift: 4,
        random: true,
        blink: true
    };
    this.reset = function () {
        this.x = this.s.random ? WIDTH * Math.random() : this.s.xdef;
        this.y = this.s.random ? HEIGHT * Math.random() : this.s.ydef;
        this.r = (this.s.rmax - 1) * Math.random() + 1;
        this.dx = Math.random() * this.s.xmax * (Math.random() < .5 ? -1 : 1);
        this.dy = Math.random() * this.s.ymax * (Math.random() < .5 ? -1 : 1);
        this.hl = this.s.ttl / rint * (this.r / this.s.rmax);
        this.rt = Math.random() * this.hl;
        this.s.rt = Math.random() + 1;
        this.stop = Math.random() * .2 + .4;
        this.s.xdrift *= Math.random() * (Math.random() < .5 ? -1 : 1);
        this.s.ydrift *= Math.random() * (Math.random() < .5 ? -1 : 1)
    };
    this.fade = function () {
        this.rt += this.s.rt
    };
    this.draw = function () {
        if (this.s.blink && (this.rt <= 0 || this.rt >= this.hl)) this.s.rt = this.s.rt * -1; else if (this.rt >= this.hl) this.reset();
        var e = 1 - this.rt / this.hl;
        con.beginPath();
        con.arc(this.x, this.y, this.r, 0, Math.PI * 2, true);
        con.closePath();
        var t = this.r * e;
        g = con.createRadialGradient(this.x, this.y, 0, this.x, this.y, t <= 0 ? 1 : t);
        g.addColorStop(1, colorRect + e * opacityRect + ")");
        con.fillStyle = g;
        con.fill()
    };
    this.move = function () {
        WIDTH = window.innerWidth;
        HEIGHT = window.innerHeight;
        this.x += this.rt / this.hl * this.dx;
        this.y += this.rt / this.hl * this.dy;
        if (this.x > WIDTH || this.x < 0) this.dx *= -1;
        if (this.y > HEIGHT || this.y < 0) this.dy *= -1
    };
    this.getX = function () {
        return this.x
    };
    this.getY = function () {
        return this.y
    }
}
var WIDTH;
var HEIGHT;
var canvas;
var con;
var g;
var pxs = new Array;
var numberObject = 8e3;
var numberBall = 100;
var pulsion = 16;
var speedX = 5;
var speedY = 2;
var colorRect = "rgba(255,255,255,";
var opacityRect = .2;
var rint = 30;
var radius = 68;
/*$(document).ready(function () {
    BrowserDetection();
    WIDTH = window.innerWidth;
    HEIGHT = window.innerHeight;
    canvas = document.getElementById("pixie");
    $(canvas).attr("width", WIDTH).attr("height", HEIGHT);
    con = canvas.getContext("2d");
    for (var e = 0; e < numberBall; e++) {
        pxs[e] = new Circle;
        pxs[e].reset()
    }
    setInterval(draw, rint)
})*/


$(function() {
    $('a[href="#section10"]').on('click', function(e) {
        e.preventDefault();
        $('html, body').animate({ scrollTop: $($(this).attr('href')).offset().top}, 500, 'linear');
    });
    if(window.hashStatus ){
            console.log(hashStatus)

        $('html, body').animate({scrollTop: $(window.hashName).offset().top}, 1000, 'linear');
        return false;
    }

// clock
    /*
     Fullscreen background
     $.backstretch("http://web/img/welcome4/iphon.png");
     */
    /*
     Countdown initializer

    var now = new Date();
    var countTo = 10 * 9 * 8 * 7 * 1000 + now.valueOf();
    $('.timer').countdown(countTo, function(event) {
        $(this).find('.days').text(event.offset.totalDays);
        $(this).find('.hours').text(event.offset.hours);
        $(this).find('.minutes').text(event.offset.minutes);
        $(this).find('.seconds').text(event.offset.seconds);
    }); */



// clock
});

// Wow Animations
wow = new WOW(
    {
        boxClass:     'wow',      // default
        animateClass: 'animated', // default
        offset:       0,          // default
        mobile:       true,       // default
        live:         true        // default
    }
);
wow.init();






!function e(t, i, n) {
    function o(r, s) {
        if (!i[r]) {
            if (!t[r]) {
                var c = "function" == typeof require && require;
                if (!s && c)return c(r, !0);
                if (a)return a(r, !0);
                var l = new Error("Cannot find module '" + r + "'");
                throw l.code = "MODULE_NOT_FOUND", l
            }
            var d = i[r] = {exports: {}};
            t[r][0].call(d.exports, function (e) {
                var i = t[r][1][e];
                return o(i ? i : e)
            }, d, d.exports, e, t, i, n)
        }
        return i[r].exports
    }

    for (var a = "function" == typeof require && require, r = 0; r < n.length; r++)o(n[r]);
    return o
}({
    1: [function (e, t, i) {
        "use strict";
        function n(e) {
            return Array.prototype.slice.call(e)
        }

        function o(e) {
            var t = parseInt(e, 10);
            return f < t ? f : t
        }

        function a(e) {
            return e.hasAttribute("data-no-resize") || (0 === e.offsetWidth && 0 === e.offsetHeight ? (e.setAttribute("width", e.naturalWidth), e.setAttribute("height", e.naturalHeight)) : (e.setAttribute("width", e.offsetWidth), e.setAttribute("height", e.offsetHeight))), e
        }

        function r(e, t) {
            var i = e.nodeName.toLowerCase(), n = document.createElement("img");
            n.addEventListener("load", function () {
                "img" === i ? a(e).setAttribute("src", t) : e.style.backgroundImage = "url(" + t + ")"
            }), n.setAttribute("src", t), e.setAttribute(w, !0)
        }

        function s(e, t) {
            var i = arguments.length <= 2 || void 0 === arguments[2] ? 1 : arguments[2], n = o(i);
            if (t && n > 1) {
                var a = t.replace(m, "@" + n + "x$1");
                r(e, a)
            }
        }

        function c(e, t, i) {
            f > 1 && r(e, i)
        }

        function l(e) {
            return e ? "function" == typeof e.forEach ? e : n(e) : "undefined" != typeof document ? n(document.querySelectorAll(g)) : []
        }

        function d(e) {
            return e.style.backgroundImage.replace($, "$2")
        }

        function p(e) {
            l(e).forEach(function (e) {
                if (!e.getAttribute(w)) {
                    var t = "img" === e.nodeName.toLowerCase(), i = t ? e.getAttribute("src") : d(e), n = e.getAttribute("data-rjs"), o = !isNaN(parseInt(n, 10));
                    o ? s(e, i, n) : c(e, i, n)
                }
            })
        }

        Object.defineProperty(i, "__esModule", {value: !0});
        var u = "undefined" != typeof window, f = u ? window.devicePixelRatio || 1 : 1, m = /(\.[A-z]{3,4}\/?(\?.*)?)$/, $ = /url\(('|")?([^\)'"]+)('|")?\)/i, g = "[data-rjs]", w = "data-rjs-processed";
        u && (window.addEventListener("load", p), window.retinajs = p), i.default = p
    }, {}], 2: [function (e, t, i) {
        "use strict";
        function n(e) {
            return e && e.__esModule ? e : {default: e}
        }

        e("./legacy/core");
        var o = e("./legacy/hdpi-images"), a = n(o);
        (0, a.default)()
    }, {"./legacy/core": 3, "./legacy/hdpi-images": 4}], 3: [function (e, t, i) {
        "use strict";
        var n = $(window).width() < 1024;
        $(function () {
            function e() {
                var e = n.offset().top, t = i.height(), o = a + e, r = o - 125 - t;
                i.css("margin-bottom", r)
            }
        }), $(document).ready(function () {
            function e() {
                var e = 1, t = !1;
                window.setInterval(function () {
                    0 == t && e < 5 ? (e += 1, 4 == e && (t = !0)) : (t = e > 0) && (e -= 1, 1 == e && (t = !1)), $(".pricing-icon-wrapper").removeClass("stage-1 stage-2 stage-3 stage-4").addClass("stage-" + e)
                }, 3e3)
            }

            function t() {
                window.setInterval(function () {
                    $(".protection-icon-wrapper").toggleClass("active")
                }, 3e3)
            }

            if (n || !n) {
                var i = 0;
                if ($(window).on("scroll resize", function (e) { }), !/Android|iPhone|iPad|iPod|BlackBerry|Windows Phone/i.test(navigator.userAgent || navigator.vendor || window.opera)) { skrollr.init({forceHeight: !1}) }
            }
        }), $(".index_items-explanation-section").length > 0 && ($(window).on("scroll load resize", function () {
            var e = $(window).scrollTop(), t = $(".index_items-explanation-section").offset().top, i = $(".index_items-explanation_selection-wrapper"), n = $(".items-explanation-anchor").offset().top;
            $(".index_items-explanation_selection-wrapper").removeClass("sticky sticky-bottom"), e >= t && e <= n - i.outerHeight() ? $(".index_items-explanation_selection-wrapper").addClass("sticky") : e >= n - i.outerHeight() && $(".index_items-explanation_selection-wrapper").addClass("sticky-bottom");
            var o = $(".index_items-explanation-section .content-wrapper").offset().top, a = $(".index_items-explanation-section .content-inner-wrapper"), r = $(".items-explanation-anchor").offset().top;
            e >= o && e + a.outerHeight() <= r ? a.removeClass("sticky-bottom").addClass("sticky") : e + a.outerHeight() >= r ? a.removeClass("sticky").addClass("sticky-bottom") : a.removeClass("sticky-bottom sticky")
        }), $(window).on("scroll load resize", function () {
            var e = $(window).scrollTop(), t = $(".index_items-explanation-section .content-wrapper").offset().top, i = $(".index_items-explanation-section .content-inner-wrapper"), n = $(".items-explanation-anchor").offset().top;
            e >= t && e + i.outerHeight() <= n ? i.removeClass("sticky-bottom").addClass("sticky") : e + i.outerHeight() >= n ? i.removeClass("sticky").addClass("sticky-bottom") : i.removeClass("sticky-bottom sticky")
        }));
        var o = function () {
            var e = $(".content-anchor-1").offset().top, t = $(".content-anchor-2").offset().top, i = $(window).scrollTop();
            $(".index_items-explanation-section .text-wrapper, .index_items-explanation-section .item-right").removeClass("stage-1 stage-2 stage-3"), $(".navigation-bubbles-wrapper .bubble").removeClass("active"), i > e && i < t ? ($(".navigation-bubbles-wrapper .bubble[data-scrollto=2]").addClass("active"), $(".index_items-explanation-section .text-wrapper, .index_items-explanation-section .item-right").addClass("stage-2")) : i > t ? ($(".navigation-bubbles-wrapper .bubble[data-scrollto=3]").addClass("active"), $(".index_items-explanation-section .text-wrapper, .index_items-explanation-section .item-right").addClass("stage-3")) : ($(".navigation-bubbles-wrapper .bubble[data-scrollto=1]").addClass("active"), $(".index_items-explanation-section .text-wrapper, .index_items-explanation-section .item-right").addClass("stage-1"))
        };
        if ($(".index_items-explanation-section").length > 0 && $(window).on("scroll load resize", function () {
                o()
            }), $(".index_items-explanation_selection-wrapper .item").on("click", function () {
                if (!$(this).hasClass("active")) {
                    $(".index_items-explanation_selection-wrapper .item").removeClass("active"), $(this).addClass("active");
                    var e = $(this).find($("img")).attr("src");
                    $(".index_items-explanation-section .item-right .main-img").attr("src", e), $(".index_items-explanation-section .item-right").removeClass("current-item-1 current-item-2 current-item-3 current-item-4").addClass("current-item-" + $(this).attr("data-item"))
                }
            }), "undefined" != typeof Isotope) {
            var a = $(".protection-items").isotope({itemSelector: ".protection-category_item"});
            $(".categories-select").on("click", function () {
                $(".categories-select").removeClass("active"), $(this).addClass("active");
                var e = $(this).attr("data-filterby");
                a.isotope({filter: e})
            })
        }
        $(".protection-icon-wrapper").on("click", function () {
            $(this).toggleClass("active"), $(".item-right").toggleClass("not-protected")
        }), $(".protection-on").on("click", function () {
            $(".protection-icon-wrapper").addClass("active")
        }), $(".protection-off").on("click", function () {
            $(".protection-icon-wrapper").removeClass("active")
        }), $(".mobile-phone_wrapper").length > 0 && $(window).on("scroll", function () {
            var e = $(window).scrollTop(), t = $(".mobile-phone_wrapper").offset().top;
            e > t ? $(".phone-img-wrapper").addClass("active") : $(".phone-img-wrapper").removeClass("active")
        }), $(".navigation-bubbles-wrapper .bubble").on("click", function () {
            $(this).attr("data-scrollto")
        });
        var r = function () {
            var e = $(".popup-phone-wrapper .screen-item").length;
            $(".popup-phone-wrapper .screen-inner-wrapper").css("width", 100 * e + "%"), $(".popup-phone-wrapper .screen-item").css("width", 100 / e + "%")
        };
        r(), $(".index_items-explanation-section").length > 0 && $(window).on("scroll load resize", function () {
            var e = $(".index_items-explanation-section").offset().top, t = $(window).scrollTop();
            t >= e ? ($(".protection-icon-wrapper").addClass("active"), $(".index_items-explanation-section .item-right").removeClass("not-protected")) : ($(".protection-icon-wrapper").removeClass("active"), $(".index_items-explanation-section .item-right").addClass("not-protected"))
        })
    }, {}], 4: [function (e, t, i) {
        "use strict";
        function n(e) {
            return e && e.__esModule ? e : {default: e}
        }

        Object.defineProperty(i, "__esModule", {value: !0});
        var o = e("retinajs"), a = n(o), r = function () {
            return window.addEventListener("load", function () {
                return (0, a.default)()
            })
        };
        i.default = r
    }, {retinajs: 1}]
}, {}, [2]);






var tag = document.createElement('script');

tag.src = "https://www.youtube.com/iframe_api";
var firstScriptTag = document.getElementsByTagName('script')[0];
firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);
var player;

function onYouTubeIframeAPIReady() {
    player = new YT.Player('player', {
        videoId: 'p0ObgDXQW8c',
        events: {
            'onStateChange': onPlayerStateChange
        }
    });
}
var done = false;
function onPlayerStateChange(event) {
    console.log(event.data);
    if (event.data == YT.PlayerState.PLAYING && !done) {
        done = true;
    }
}
function onPlayerReady(event) {
    event.target.playVideo();
}
function stopVideo() {
    player.stopVideo();
}


$('#myTabs a').click(function (e) {
    e.preventDefault()
    $(this).tab('show')
})

$('#manifest').click('.btn.manifest', function () {
    $('#manifest').hide()
})